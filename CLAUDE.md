# CLAUDE.md

이 파일은 Claude Code가 세션 시작 시 자동으로 읽는 프로젝트 메모리다. 짧게 유지하고,
세부 사항은 아래 "상세 스펙" 문서들을 **필요할 때 직접 열어서** 확인한다(이 파일에
전부 옮겨 적지 않는다 — 중복되면 나중에 어긋난다).

## 프로젝트 현재 상태 (중요 — 오해하기 쉬운 지점)

- **CogniTrieve**는 세종대 "창의학기제"(2026-1학기, 학생 자율 프로젝트 과목)에서
  시작됐고, **그 과목 자체는 이미 종료되었다.**
- 지금(2026년 여름방학) 진행 중인 확장은 **학교 프로그램의 연장이 아니라, 개발자
  본인의 개인 포트폴리오 프로젝트**다. 취업 준비를 위해 자율적으로 시간을 들여
  개선·확장하고 있다. 1학기 팀원 중 한 명은 프로젝트 초반에 이탈해 현재 실질적으로
  1인 개발 체제다.
- 창의학기제를 지도해주셨던 교수님이 **방학 중에도 개인적 선의로 조언을 주고
  계시는 관계는 남아 있다** — 다만 이는 공식적인 학사 절차(지도·보고 의무)가
  아니라 사적인 도움이다.
- **따라서** "지도교수님께 확인/보고" 같은 문구가 다른 문서(`docs/10` 등)에 남아있어도,
  이는 학교 행정 절차가 아니라 **개발자 본인이 필요하면 개인적으로 여쭤보는 조언
  루트**로 이해한다. IRB(기관생명윤리위원회) 심의 같은 항목도 공식 학교 프로그램
  소속 여부에 따라 실제로는 적용되지 않을 수 있다 — 단정하지 말고 실행 전 사용자에게
  다시 확인한다.
- 개발 체제: **1인 개발**.

## 기술 스택

Python, Streamlit(유지 결정 — React/FastAPI 등으로의 전환은 검토 후 기각됨),
Google Generative AI(Gemini API), Supabase(Postgres, 파일럿 데이터 축적용으로 추가).

## 상세 스펙은 여기서 찾을 것 (필요할 때 Read)

- **`AI_INSTRUCTIONS.md`** — 아키텍처 규칙, 디렉토리 구조, 파이프라인 1~3단계
  데이터 스펙. **코드를 고치기 전에 항상 먼저 확인.**
- **`docs/10_구현로드맵.md`** — 지금 어느 구현 Phase(1~5)를 하고 있는지, 각 Phase의
  작업 항목·완료 기준. **작업 시작 전 이 파일의 체크박스 상태를 확인하고, 완료된
  항목을 체크 표시로 갱신한다.**
- **`docs/README.md`** — 설계 로드맵(Phase 0~7.2)과 구현 로드맵 전체 현황 표.
- **`docs/04~09`** — 특정 설계 판단("왜 이 임계값인지", "왜 이 문항인지")의 근거가
  필요할 때만 연다. 목록은 `AI_INSTRUCTIONS.md` 상단 "설계 근거" 참조.

## 반드시 지킬 것 (모든 세션 공통)

1. **설계 문서가 스펙이다.** 임계값·문항·위험도 등급을 코드에서 임의로 새로 만들지
   않는다. 문서에 없으면 사용자에게 묻는다.
2. **케이스(`case_id`)를 하드코딩하지 않는다.** `Q2`, `Q4` 같은 특정 케이스 전용 값을
   코드에 박지 않고, 항상 `cases` 테이블/설정에서 조회한다.
3. **한 번에 구현 Phase 하나씩.** `docs/10`의 Phase 순서를 따르고, 이전 Phase 완료
   기준을 만족하기 전에 다음 Phase로 넘어가지 않는다.
4. **작업 전 Plan 모드로 계획을 먼저 보여준다.** 특히 여러 파일에 걸친 변경이나 DB
   스키마 변경 전에는 반드시 계획을 먼저 제시하고 승인을 받는다.
5. **커밋은 Phase/작업 단위로 작게, 브랜치는 `feature/impl-phase{N}-{요약}` 형식.**
   이 프로젝트는 지금까지 문서 작업(`docs/` 커밋)과 코드 작업의 커밋 메시지 스타일을
   구분해왔다 — 코드 작업은 `feat(...)`/`fix(...)` 등 통상적인 conventional commit
   접두사를 쓴다.
6. **Supabase는 dev/prod를 분리해서 쓴다.** 개발 중에는 항상 dev 프로젝트만 건드린다.
